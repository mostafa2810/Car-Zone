import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class oldcar extends StatelessWidget{
  static const String routeName='oldcar';
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}